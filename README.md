# Web-Browser-OS-Project
OS Text Browser is a terminal-launched, GUI-based web browser written entirely in C, built as a university Operating Systems project by Nimrita Baby &amp; Wishma. It simulates how a real browser isolates tabs, fetches pages, and manages concurrency using nothing but standard POSIX system calls and the GTK3 toolkit.
